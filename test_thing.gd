extends Node2D

#@export var manager: FightManager

func _ready():
	pass
	#var alive = true
	#while alive:
		#fighter.take_wounds(1, '{target} got poked in the {part} for {dmg}'.format({"target": fighter.name}))
		#var knocked = fighter.check_knockout()
		#print('knocked? ', knocked)
		#alive = !knocked
	#var died = fighter.check_died()
	#print('died? ', died, {
		#"wounds": fighter.total_wounds(),
		#"tough": fighter.tough
	#})
